# pocket-option-analyzer
Chart image analyzer for Pocket Option
# PO Analyzer

Simple GUI app (Tkinter) that uploads a chart image to a Gradio Space and shows CALL/PUT result.

## Build
The repository contains a GitHub Actions workflow that builds a single-file EXE using PyInstaller.
Push to `main` to trigger CI and download the produced artifact from Actions -> latest run -> Artifacts.
